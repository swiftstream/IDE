export class AndroidAppProject {
    static generateIfNeeded(options: {
        projectPath: string,
        package: string,
        name: string,
        targets: string[],
        compileSdk: number,
        minSdk: number,
        javaVersion: number,
        swiftVersion: string
    }) {

    }
}